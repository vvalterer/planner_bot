# 🤖 AI Content Planner Bot
![CI](https://github.com/vvalterer/planner_bot/actions/workflows/ci.yml/badge.svg)

**Вячеслав Ветошкин** | [Сайт](https://1vetoshkin.ru) | [Telegram](https://t.me/TkAs007bot)

Бот для генерации контент-планов с использованием AI (шаблоны/GigaChat).
Разработан для Telegram, поддерживает администрирование и подписки.

## 🚀 Быстрый запуск

### Локально
```bash
# Клонируем и настраиваем
git clone <repo-url>
cd planner_bot

# Создаём виртуальное окружение
python -m venv .venv
.venv\Scripts\activate  # Windows
# source .venv/bin/activate  # Linux/Mac

# Устанавливаем зависимости
pip install -r requirements.txt

# Настраиваем .env
copy .env.example .env
# Вставьте BOT_TOKEN от @BotFather

# Запускаем
python app/main.py
```

### Docker
```bash
docker-compose up -d --build
```

## 📦 Структура проекта

```
app/
├── main.py           # Точка входа
├── config.py         # Конфигурация
├── handlers/         # Обработчики команд
│   ├── feature.py    # Основная логика
│   └── help_text.py  # Текст справки
├── services/         # Бизнес-логика
│   └── planner.py    # Генерация планов
├── database/         # Работа с БД
│   ├── connection.py # Подключение
│   └── repository.py # CRUD операции
└── keyboards/        # Клавиатуры
    └── main.py       # Reply/Inline KB
tests/                # Тесты pytest
```

## ⚙️ Настройки (.env)

```ini
BOT_TOKEN=your_telegram_bot_token
ADMIN_IDS=123456789,987654321
DB_PATH=data/database.sqlite3
LOG_LEVEL=INFO
```

## 🧪 Тестирование

```bash
pytest tests/ -v
```

## 📋 Команды бота

### Основные
- `/start` — запуск бота
- `/help` — справка
- `/buy` — оформить подписку (тест)

### Для администраторов
- `/admin` — статистика бота
- `/broadcast <текст>` — рассылка всем пользователям

Напишите нишу и ЦА для генерации плана:
```
ниша: фитнес, ЦА: женщины 25-35
```

## 💳 Система подписки
Бот работает по подписке. 
- Пробный период: отсутствует (реализовать при необходимости)
- Оплата: тестовая заглушка (начисляется 30 дней)

## 🐳 Деплой (Production)

### Файловая структура на сервере
- `docker-compose.yml`
- `.env`
- `entrypoint.sh`

### Запуск
```bash
docker-compose up -d
```
Данные сохраняются в `./data`. Логи ротируются автоматически.

---
© 2025 Вячеслав Ветошкин
